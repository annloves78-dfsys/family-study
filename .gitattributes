# 서버(리눅스)에서 실행되는 파일은 반드시 LF 로 유지합니다.
# CRLF 로 체크아웃되면 쉘 스크립트가 "bad interpreter" 로 죽습니다.
*.sh          text eol=lf
*.sql         text eol=lf
*.service     text eol=lf
*.conf        text eol=lf
deploy/**     text eol=lf
server/**     text eol=lf

# 나머지는 git 기본 동작
* text=auto
